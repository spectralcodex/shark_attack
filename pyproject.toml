[project]
name = "shark-attack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1.3",
    "pyodbc>=5.3.0",
    "pytest>=8.4.2",
]

[dependency-groups]
dev = [
    "pytest-cov>=7.1.0",
]
